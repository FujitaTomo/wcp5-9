<h1>Profile Edit</h1>
	<%= form_for(@user) do |f| %>
		<h4>Name</h4>
		<%= f.text_field :name %>
		<h4>Image</h4>
		<%= f.attachment_field :user_image %>
		<h4>Introduction</h4>
		<%= f.text_area :introduction %>
		<br><br>

		<%= f.submit "submit", class: 'btn btn-primary' %>
	<% end %>
